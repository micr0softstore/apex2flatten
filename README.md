# apex2flatten
A simple tool written in python to extract android .apex files on Linux.

Useage:

sudo python3 apex2flatten.py --input /path/to/your.apex

Output:
/yourapex (the final extracted apex)
/apex_config (Permission config for the extracted apex)
/apex_secontexts (Secontext config for the extracted apex)

PRs are welcome.
